<%- include('./partials/header') %>
<div class="max-w-3xl mx-auto p-6 bg-white rounded-lg shadow-md mt-16">
    <h1 class="text-2xl font-bold mb-4">Order Placed</h1>
    <p class="text-gray-700">Thank you, <%= user.name %>! Your order has been placed successfully.</p>
    <div class="mt-6">
        <h2 class="text-lg font-semibold">Order Details</h2>
        <p><strong>Order ID:</strong> <%= order._id %></p>
        <p><strong>Product Name:</strong> <%= product.name %></p>
        <p><strong>Quantity:</strong> <%= order.quantity %></p>
        <p><strong>Total Amount:</strong> ₹<%= order.payableAmount %></p>
    </div>
    <div class="mt-6">
        <img src="data:image/jpeg;base64,<%= product.image.toString('base64') %>" alt="Product Image" class="w-20 h-20 border rounded-md">
    </div>
</div>

